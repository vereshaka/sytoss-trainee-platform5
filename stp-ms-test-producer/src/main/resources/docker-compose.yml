version: "3.7"
services:
  mongo:
    container_name: mongo_stub
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: pwd
      MONGO_INITDB_DATABASE: stp
    ports:
      - 27017:27017